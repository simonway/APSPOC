# 2026-05-11 发布验收 smoke 记录

> 说明：本文档记录 `2026-05-11` 在本地 `frontend 8080 / backend 8081 / solver 8000` 全部启动后的发布验收 smoke。
> 它用于补充 [2026-05-09_二期路线图闭环更新.md](/Users/simon/Documents/MyProgramming/APSPOC/docs/release/2026-05-09_二期路线图闭环更新.md:1) 的验证结果，给出当前代码基线的最新实测记录。

## 1. 结论

截至 `2026-05-11`，当前代码基线已通过本地发布验收 smoke：

- `./scripts/verify-release.sh --smoke-only` 全部通过
- `rtk mvn test` 全量 backend 测试通过，`44` 个测试全绿
- `rtk node --check backend/src/main/resources/static/app.js` 通过

当前建议继续沿用的对外口径是：

> 二期路线图闭环完成，系统可进入业务演示、UAT 与发布验收。

## 2. 本次 smoke 覆盖范围

- 四类角色登录与会话校验：`Admin / Planner / Approver / Viewer`
- 样例排程提交、轮询完成、生成新版本
- 版本 diff、版本 history、全局 audit history
- Excel 模板导出
- `resources / tasks / downtimes` 导入 smoke
- 物料流可行 / 不可行场景 smoke
- `Ready for Release -> Reject -> Ready for Release -> Approve -> Publish -> Rollback`
- `UAT_v1_sample_package` 从导入批次到正式排程的全链路 smoke

## 3. 关键结果

### 3.1 角色与主链路

- `admin / planner / approver / viewer` 四个默认账号均登录成功，`session` 返回的 `role` 与预期一致
- 样例排程由 `planner` 提交：
  - `jobId = job-f37cf5ba-aa31-42c8-a68a-07a90d618faf`
  - 最终状态：`SUCCEEDED / OPTIMAL`
  - 生成版本：`ver-a65cf751-238f-40d8-8604-b66216428b8e`
- 版本总数由 `15` 增加到 `16`

### 3.2 导入、模板与物料流

- Excel 模板导出成功：
  - `contentType = application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
  - `size = 8652`
- 导入 smoke 成功：
  - `resources = 1`
  - `tasks = 1`
  - `downtimes = 1`
- 物料流可行场景：
  - 场景：`scn-dffcd66a-21a1-43cc-8b4c-512ae7c884bf`
  - 作业状态：`SUCCEEDED / OPTIMAL`
  - 版本：`ver-7540c41f-81c1-447e-93ba-7bb9c57b7962`
- 物料流不可行场景：
  - 作业状态：`FAILED / INFEASIBLE`
  - 错误信息：`Solver finished without a feasible schedule: INFEASIBLE`
- 对不可行场景执行 `retry` 后，仍稳定返回同样的 `FAILED / INFEASIBLE`，符合预期

### 3.3 版本治理

- `Release Note` 保存成功：`Release closure smoke note`
- 状态流转成功：
  - `READY_FOR_RELEASE`
  - `REJECTED`
  - `READY_FOR_RELEASE`
  - `APPROVED`
  - `RELEASED`
- 版本历史中已看到：
  - `PUBLISH`
  - `APPROVE`
  - `READY_FOR_RELEASE`
  - `REJECT`
- `auditHistoryCount = 20`
- `rollback` 成功恢复到：
  - `targetVersionId = ver-50703312-dcbb-4e85-a6b1-0a83712b4755`
  - 状态：`RELEASED`

### 3.4 UAT 样包全链路

- 样包目录：
  - `docs/sample-packages/UAT_v1_sample_package`
- 六类导入全部 `VALIDATED`，且 `failureCount = 0`
- 场景结果：
  - `scenarioId = scn-d856c8c4-8c32-4eaf-9f63-c7792751a8c2`
  - `resourceCount = 10`
  - `demandCount = 8`
  - `operationCount = 22`
  - `precedencePairCount = 14`
  - `bridgeAdjustmentCount = 1`
- 正式排程结果：
  - `jobId = job-6adcaa2c-11f3-44a5-a83a-7034d2f19f51`
  - `status = SUCCEEDED`
  - `solverStatus = OPTIMAL`
  - `versionId = ver-c9f9a69b-5240-4c8a-ae10-607546ace077`

## 4. 验证产物

- [release-smoke-summary.json](/Users/simon/Documents/MyProgramming/APSPOC/.runtime/release-smoke-summary.json:1)
- [uat-package-smoke-summary.json](/Users/simon/Documents/MyProgramming/APSPOC/.runtime/uat-package-smoke-summary.json:1)

## 5. 备注

- `2026-05-09_二期路线图闭环更新.md` 中提到的 `ModelImportServiceTest` 本机 `Abort trap: 6` 既有噪音，已在 `2026-05-11` 消除
- 本次修正后，`rtk mvn test` 已可直接覆盖 `ModelImportServiceTest` 在内的全量 backend 测试
- 本文档只更新“当前验证结果”，不改写 `2026-05-08` 与 `2026-04-27` 历史记录的原始结论
