# 2026-04-27 发布收口验收记录

> 说明：本文档是 `2026-04-27 / v0.1.0` 的历史发布基线记录，主要证明当时的主链路 smoke 已成立。
> 它不代表当前“二期/M2-M5”已全部完成。
> 当前阶段完成度请参考 [2026-05-09_二期路线图闭环更新.md](/Users/simon/Documents/MyProgramming/APSPOC/docs/release/2026-05-09_二期路线图闭环更新.md:1)。
> 最新本地发布验收结果请参考 [2026-05-11_发布验收_smoke_记录.md](/Users/simon/Documents/MyProgramming/APSPOC/docs/release/2026-05-11_发布验收_smoke_记录.md:1)。

## 1. 执行范围

- 环境健康检查：frontend `8080`、backend `8081`、solver `8000`
- 核心业务 smoke：登录、会话、版本列表、样例排程、任务轮询、版本 diff
- 版本治理 smoke：版本说明、提交待发布、发布、回退、审计历史
- 导入导出 smoke：模板导出、资源导入、任务导入、停机窗口导入
- 自动化回归：backend 单元测试、solver 单元测试、frontend 语法检查、`git diff --check`

## 2. 本次执行结果

执行日期：`2026-04-27`  
执行分支：`dev`

### 2.1 服务健康检查

- frontend `8080`：监听正常，`GET /` 返回 `200`
- backend `8081`：监听正常，`GET /actuator/health` 返回 `{"status":"UP"}`
- solver `8000`：监听正常，`GET /health` 返回 `{"status":"UP"}`

### 2.2 核心业务 smoke

- 登录：`POST /api/v1/auth/login` 返回 `200`
- 会话：`GET /api/v1/auth/session` 返回 `200`
- 版本列表：排程前版本总数 `25`
- 样例排程提交：`POST /api/v1/schedule/jobs/sample` 返回 `202`
- 样例任务结果：`job-87de8329-eaf4-414b-a123-f8c703e84952` 最终状态 `DONE / OPTIMAL`
- 新生成版本：`ver-a3458cbb-27c9-47e2-a9d0-ec2665805066`
- 排程后版本总数：`26`
- 版本 diff：最新版本相对上一个版本返回 `200`
  - `changedTaskCount = 2`
  - `addedTaskCount = 0`
  - `removedTaskCount = 0`

### 2.3 版本治理 smoke

- 发布前生效版本：`ver-50703312-dcbb-4e85-a6b1-0a83712b4755`
- 保存版本说明：`PUT /release-note` 返回 `200`
- 提交待发布：`POST /ready-for-release` 返回 `200`，状态变为 `READY_FOR_RELEASE`
- 发布：`POST /publish` 返回 `200`，目标版本状态变为 `RELEASED`
- 版本历史：返回最近事件 `PUBLISH`、`READY_FOR_RELEASE`
- 审计历史：当前记录数 `5`
- 回退：`POST /rollback` 返回 `200`，目标版本重新进入 `RELEASED`

### 2.4 导入导出 smoke

- 模板导出：`GET /api/v1/model-import/template` 返回 `200`
  - `Content-Type = application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
  - 文件大小 `5734` 字节
- 资源最小导入：返回 `200`，导入 `1` 行
- 任务最小导入：返回 `200`，导入 `1` 行
- 停机窗口最小导入：返回 `200`，导入 `1` 行

## 3. 自动化回归结果

已执行：

- `./scripts/verify-release.sh --tests-only`
- `./scripts/verify-release.sh --smoke-only`

当前结论：

- backend 单元测试：通过
- solver 单元测试：通过
- frontend 语法检查：通过
- diff 格式检查：通过

## 4. 当前结论

- API 主链路与版本治理主链路已通过本地 smoke
- 导入导出最小闭环已通过本地 smoke
- 当前版本具备发布收口基础

## 5. 仍建议人工补走的 UI 场景

- 浏览器登录页与工作台双语切换
- Gantt 任务拖拽、跨行移动、锁定/解锁、`Ctrl+Z`
- “变更任务”点击后自动定位与自动解锁联动
- 账号设置弹层与退出登录入口
- 浏览器强刷后的静态资源缓存生效
