# 2026-05-11 `v0.2.0` 转正式版最终检查清单

## 1. 适用范围

本清单用于把 GitHub 上已经创建的 `pre-release V0.2.0`，转为正式 release 之前的最后确认动作。按 Snow Beer 五期规划口径，只有第五期发布级产品化与 release gate 无阻断项后，才执行本清单。

当前已知基线：

- GitHub 当前状态：`pre-release V0.2.0`
- Git tag：`v0.2.0`
- 发布基线：`master` 上的 `f89fe3f`

说明：

- 如正式发布仍沿用当前 `v0.2.0` tag，则正式版应继续对应 `f89fe3f`
- 如在正式发布前又发生需要进入正式版的代码修复，应重新评估是否沿用 `v0.2.0`，还是改为新 tag 后再发布

## 2. 必过检查

- 已确认 [2026-05-09_二期路线图闭环更新.md](/Users/simon/Documents/MyProgramming/APSPOC/docs/release/2026-05-09_二期路线图闭环更新.md:1) 仍可代表当前二期范围结论
- 已确认 [2026-05-11_发布验收_smoke_记录.md](/Users/simon/Documents/MyProgramming/APSPOC/docs/release/2026-05-11_发布验收_smoke_记录.md:1) 中记录的测试结果仍有效，且没有新的阻断缺陷
- 已确认 `rtk mvn test`、`rtk node --check backend/src/main/resources/static/app.js`、`./scripts/verify-release.sh --smoke-only` 对应的验收结论已被审阅
- 已确认 Snow Beer 第三期主链路演示已验收通过，第四期全业务场景补齐和第五期发布级产品化无剩余阻断项
- 已确认 `UAT_v1_sample_package`、`snow_beer_main_chain_phase3` 或后续 Snow Beer 样本的端到端结果可作为正式发布前的业务演示样本，且不会把第三期主链路样本误表述为第四期全业务场景
- 已确认当前对外口径仍是：

> 二期路线图闭环完成，系统可进入业务演示、UAT 与发布验收。

## 3. 业务与环境确认

- 已确认业务演示或 UAT 中没有新的 `P0 / P1` 阻断问题
- 已确认当前已知限制已被接受，不会在正式发布说明中被误表述为“已完全产品化”
- 已确认是否需要先执行 [2026-05-11_发布前测试数据清理清单.md](/Users/simon/Documents/MyProgramming/APSPOC/docs/release/2026-05-11_发布前测试数据清理清单.md:1) 中的数据清理动作
- 已确认正式发布所面向的环境中，默认账号、角色、演示数据、数据库快照与回退方案都已准备好

## 4. Git 与 Release 核对

- 已确认 GitHub `pre-release V0.2.0` 对应的源码仍是 tag `v0.2.0`
- 已确认 tag `v0.2.0` 仍指向 `f89fe3f`
- 已确认正式发布时不会误把 `dev` 上后续文档或开发提交重新解释为 `v0.2.0` 的发布内容
- 已确认 `master` 没有在 `f89fe3f` 之后追加需要并入正式版的代码提交

## 5. 切换正式版时的动作

在 GitHub 上把 `pre-release V0.2.0` 转为正式 release 时，应同步记录：

- 实际转正式的日期
- 操作人
- 对应环境或演示库快照
- 是否保留 `Latest release`
- 如有补充说明，记录正式发布备注

建议同时更新：

- [2026-05-11_v0.2.0_pre-release_状态说明.md](/Users/simon/Documents/MyProgramming/APSPOC/docs/release/2026-05-11_v0.2.0_pre-release_状态说明.md:1)
- [版本发布治理清单.md](/Users/simon/Documents/MyProgramming/APSPOC/docs/release/版本发布治理清单.md:1)

## 6. 不满足条件时的处理

- 若发现新的阻断问题，不要直接把 `pre-release` 改为正式版
- 若修复只发生在 `dev`，应重新走发布整理、验证、合并、打 tag 与 release 更新流程
- 若仅有文档更新，不自动改变 `v0.2.0` 对应的发布代码基线
