# Snow Beer Packaging V1 Backend Smoke Notes

## Run Snapshot

- run time: `2026-05-06`
- package: `docs/sample-packages/snow_beer_packaging_v1`
- backend: `current code on :18080`
- dataVersion: `snow_beer_packaging_v1_2026_05_06_smoke_306d0cea`
- scenarioId: `scn-9d34157a-ee68-409b-bef9-b85c4a32dce4`
- jobId: `job-b145c362-0e20-4fbf-b1bf-1182083bd54a`
- versionId: `ver-d26bf3be-6c56-4742-a033-9c31faf7f2da`
- result: `DONE / FEASIBLE`

## Full-Chain Result

- `resources / recipes / demands / downtimes / setup-rules / inventory-balances` all imported with `VALIDATED` status and `0` failures.
- the sample package now passes the smoke script without pre-converting CSV files; the script accepts `utf-8-sig / utf-8 / gb18030 / gbk`.
- scenario generation succeeded with:
  - `requestedDemandQuantity = 73`
  - `inventoryCoveredQuantity = 26`
  - `plannedDemandQuantity = 47`
  - `operationCount = 47`
- schedule job completed successfully and persisted one draft version.

## Business-Ready Observations

- inventory-covered demand is now visible in the version view:
  - the persisted `sourceRequestJson` contains `objectiveWeights.earliness = 2`
  - the persisted `inventoryDemands` now contain `priority`
  - `GET /api/v1/versions/ver-d26bf3be-6c56-4742-a033-9c31faf7f2da/gantt-data` returns `inventoryCoverageCount = 10`
  - `dem_a500_spot_1224` is fully covered by opening finished-goods inventory with `coveredQuantity = 2 / 2`
- “avoid too-early production” is materially improved on the same sample:
  - previous baseline version `ver-5d672c52-dc8a-424e-9860-23b8bf0f0420` average terminal-task earliness: `4431.5 min`
  - current version `ver-d26bf3be-6c56-4742-a033-9c31faf7f2da` average terminal-task earliness: `608.3 min`
  - previous max earliness: `7410 min`
  - current max earliness: `1950 min`
  - `12/29` due lots are no longer front-loaded into `12/24-12/25`; the worst pull-ahead now lands on `12/28`

## KPI Snapshot

- `totalWeightedTardiness = 0`
- `lateTaskCount = 0`
- `totalMakespan = 7920 min`
- `averageUtilization = 0.4306`

## Interpretation

- the longer makespan and lower utilization are expected side effects of the new earliness penalty: the solver is now willing to keep capacity idle in order to avoid producing far ahead of demand.
- inventory-covered orders remain non-capacity markers, not physical task bars. This is the correct business interpretation for opening-stock fulfillment.
- this package still stays within the current capability boundary: packaging only, no multi-stage brewery chain, no hard release-order windows.

## Demo Notes

- for UAT or customer demo, use versions generated by the current backend process. Older versions created before the `earliness / inventory coverage` update will not show the same marker and timing behavior.
- the clearest walkthrough is:
  - first show the temporary order `dem_a500_spot_1224` as an inventory-coverage marker
  - then show that `12/29` due lots are pulled close to `12/29`, not stacked at the beginning of the horizon
  - finally show setup and downtime bars on `包装2线 / 包装3线`
